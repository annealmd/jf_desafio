name: build

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: wangchucheng/git-repo-sync@master
        with:
          target-url: "https://gitlab.com/daniel.azevedo/test-repo-sync"
          target-username: "daniel.azevedo"
          target-token: "glpat-dmcxLjKgx31pt2Tym-oS"
