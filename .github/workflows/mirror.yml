name: build

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: wangchucheng/git-repo-sync@master
        with:
          target-url: 'https://gitlab.com/annealmd/jf_desafio.git'
          target-username: 'annealmd'
          target-token: 'glpat-f9rahvBSaeCwgkdnVVwE'
